#include <stdio.h>
#include "main.h"

/**
* add - add two integers and retuns the result
* @a: var define the first digit
* @b: var define the second digit
* main.h - _putchar execute the putchar no use putchar
* Return:the function all return 0
*/

int add(int a, int b)
{
return (a + b);
}
