#include <stdio.h>
#include "main.h"

/**
* mul - return multiplies var one and var two
* @a: var one
* @b: var two
* return: return the multiplies
*/

int mul(int a, int b)
{
int result = a * b;

return(result);
}
